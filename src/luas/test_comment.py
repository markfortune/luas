def test_fn(R):
    """Testing if readthedocs will display this comment.
    
    Args:
        R (JAXArray): Array of shape (N_l, N_t)
        
    Returns:
        JAXArray: The input matrix unchanged.
    """
    
    return R